%section
  %h1 Meetings

  %ul
    - for meeting in @meetings do
      %li= link_to meeting.title, meeting, :rel => 'popover', 'data-content' => meeting.agenda, :title => meeting.location

  = link_to 'new meeting', new_meeting_path, :class => 'btn'
